// #include <iostream>
//#include <fstream>
//#include <string>

using namespace std;

//int main() 
/*
{ 
    int i = 0;
    string arr1[100];
    string names;
    try {
        ifstream file("names.data.txt");//file retreiveal 
        if (!file.good()) throw string("Cannot open data file "); // if file (not good) throw alert
        file >> arr1[i];
       //while loop to count each line from the name file 
        while (getline(file, names)) {
            if (i > 50) throw string("*** Exception *** array capacity exceeded ");
            i++;
            

        }
            //cout << " file openned successfully\n";

        //for (int x = 0; x <= i;) {

          
            //cout << arr1[x] << endl;

            /* int z = x + 1;

             if (arr1[x] > arr1[z]) {
                 cout << arr1[x] << endl;
                 x++;

             }
             if (arr1[i] <  arr1[z]) { cout << arr1[z] << endl; x++; }
         }


       // }



    } catch (string alert) { cout << alert << endl; } */
//}
